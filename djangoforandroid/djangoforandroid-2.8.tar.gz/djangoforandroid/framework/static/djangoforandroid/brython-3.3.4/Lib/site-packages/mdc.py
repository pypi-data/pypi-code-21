from browser import document, window
from tools import Tools

########################################################################
class MDC(Tools):
    """"""

    #----------------------------------------------------------------------
    def __init__(self):
        """"""


        #for element in self.jq(".mdc-icon-toggle"):
            #window.mdc.iconToggle.MDCIconToggle.attachTo(element)

        #for element in self.jq(".mdc-slider"):
            #window.mdc.slider.MDCSlider.attachTo(element)

        #for element in self.jq(".mdc-textfield"):
            #window.mdc.textfield.MDCTextfield.attachTo(element)

        #for element in self.jq(".mdc-linear-progress"):
            #window.mdc.linearprogress.MDCLinearProgress.attachTo(element)

        #for element in self.jq(".mdc-tab-bar"):
            #window.mdc.tabs.MDCTabBar.attachTo(element)





    #----------------------------------------------------------------------
    @classmethod
    def linearProgress(self, selector):
        """"""
        el = document.querySelector(selector);
        if not el:
            return None

        MDCLinearProgress = window.mdc.linearProgress.MDCLinearProgress
        linear_progress = MDCLinearProgress.new(el)

        return linear_progress.foundation_



    ##----------------------------------------------------------------------
    #def change_tab(self, event):
        #""""""

        #Tools.jq(".mdc-tab-bar .mdc-tab").removeClass("mdc-tab--active")
        #Tools.jq(event.target).addClass("mdc-tab--active")

        #print("hkgj")


    #$(".mdc-tab-bar .mdc-tab").on "click", (event) ->
        #$(".mdc-tab-bar .mdc-tab").removeClass("mdc-tab--active")
        #$(@).addClass("mdc-tab--active")